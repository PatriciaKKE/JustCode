vich_uploader:
    db_driver: orm
    mappings:
        application_cv:
            uri_prefix: /uploads/cv
            upload_destination: '%kernel.project_dir%/public/uploads/cv'
        application_cover_letter:
            uri_prefix: /uploads/cover_letters
            upload_destination: '%kernel.project_dir%/public/uploads/cover_letters'